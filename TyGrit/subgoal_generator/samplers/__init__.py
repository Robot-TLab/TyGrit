"""Samplers — candidate goal generation for subgoal generators.

Each sampler produces candidate configurations (grasp poses, placement
locations, observation viewpoints) that task-specific BT nodes consume.
"""
